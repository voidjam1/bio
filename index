<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome to my Space ✨</title>
    <style>
        /* 🎨 颜色方案：在这里一键更改 */
        :root {
            --bg-color: #f8f9fa;          /* 全局背景：浅灰色 */
            --card-bg: #ffffff;           /* 卡片背景：白色 */
            --accent-color: #6c5ce7;      /* 强调色：紫色（可以换成你喜欢的颜色） */
            --text-main: #2d3436;         /* 主标题颜色 */
            --text-sub: #636e72;          /* 正文颜色 */
            --border-radius: 20px;
        }

        body {
            font-family: 'Inter', -apple-system, sans-serif;
            background-color: var(--bg-color);
            color: var(--text-main);
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 450px;
            width: 100%;
            text-align: center;
        }

        /* 个人卡片主体 */
        .profile-card {
            background: var(--card-bg);
            padding: 40px 30px;
            border-radius: var(--border-radius);
            box-shadow: 0 15px 35px rgba(0,0,0,0.05);
            transition: transform 0.3s ease;
        }

        .profile-card:hover {
            transform: translateY(-5px);
        }

        /* 头像 */
        .avatar {
            width: 110px;
            height: 110px;
            border-radius: 50%;
            border: 4px solid var(--accent-color);
            padding: 5px;
            margin-bottom: 20px;
            object-fit: cover;
        }

        h1 {
            margin: 10px 0;
            font-size: 1.6rem;
            letter-spacing: -0.5px;
        }

        .tagline {
            color: var(--accent-color);
            font-weight: 600;
            font-size: 0.9rem;
            margin-bottom: 20px;
            text-transform: uppercase;
        }

        .bio {
            line-height: 1.7;
            color: var(--text-sub);
            font-size: 0.95rem;
            margin-bottom: 30px;
            text-align: left;
        }

        /* 按钮设计 */
        .contact-btn {
            display: inline-block;
            background-color: var(--accent-color);
            color: white;
            text-decoration: none;
            padding: 12px 30px;
            border-radius: 50px;
            font-weight: bold;
            font-size: 0.9rem;
            transition: opacity 0.2s;
            box-shadow: 0 5px 15px rgba(108, 92, 231, 0.3);
        }

        .contact-btn:hover {
            opacity: 0.9;
        }

        .footer {
            margin-top: 25px;
            font-size: 0.8rem;
            color: #bdc3c7;
        }
    </style>
</head>
<body>

    <div class="container">
        <main class="profile-card">
            <img src="https://voidjam1/bio/uni.JPG" alt="Avatar" class="avatar">
            
            <div class="tagline">果酱狗钢结构郭家沟高级工高价格赶紧滚工具柜</div>
            <h1>VOID JAM</h1>
            
            <div class="bio">
                <p>你好我是虚空果酱/果酱狗</p>
                <p>如你所见我注册了一个github账号来搞诡异低脂网页小游戏。</p>
                <p>大多数网页排版和技术支持都是gemini🙏文案我写的</p>
                <p>本质上是搞oc的以及半个画手。话虽这么说但我oc搞得也不怎么样画也画得一般</p>
                <p>1</p>
                <p>2</p>
                <p>3</p>
      </div>

            <b>联系方式：voidjam1@protonmail.com</b>
        </main>

        <footer class="footer">
            谢谢你愿意来了解我！ps：界面里可以玩
        </footer>
    </div>

</body>
</html>
